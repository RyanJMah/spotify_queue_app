function main() {
	// crude way of doing this, but ok for now
	setInterval(
		function() { window.location.reload(); },	// refresh every minute
		15*1000
		// 1000
	);
}
main();function main() {
	// crude way of doing this, but ok for now
	setInterval(
		function() { window.location.reload(); },	// refresh every minute
		10*1000
		// 1000
	);
}
main();